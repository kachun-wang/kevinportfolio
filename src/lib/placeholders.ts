/**
 * Fallback image used when project images (e.g. /projects/project-N/hero.webp) are missing.
 * Replace with a dedicated placeholder asset under public/ if desired.
 */
export const PROJECT_IMAGE_PLACEHOLDER = "/profile.png";
